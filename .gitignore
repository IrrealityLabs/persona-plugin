# Runtime user data (personas + study outputs live in the user's project, never in the plugin)
.personas/
.persona-research-runs/
.env

# Python
__pycache__/
*.pyc
*.egg-info/
.venv/
venv/

# Node
node_modules/

# OS / editor
.DS_Store
*.swp

# Local-only design notes (never pushed)
*.local.md

# CLAUDE.md is local Claude guidance containing Rally strategy — never push to the public repo
/CLAUDE.md
