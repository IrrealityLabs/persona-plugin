# Runtime user data (personas + study outputs live in the user's project, never in the plugin)
.personas/
.persona-research-runs/
.env

# Local-only eval staging — raw study data + notes, never committed alongside the plugin
dev/

# Python
__pycache__/
*.pyc
*.egg-info/
.venv/
venv/

# Node
node_modules/

# OS / editor
.DS_Store
*.swp

# Local-only design notes (never pushed)
*.local.md

# CLAUDE.md is local Claude guidance — never push to the public repo
/CLAUDE.md
